# Ipopt run logs (per-run, not source)
ipopt_*.log

# Profile binaries (machine-specific, large)
*.pb.gz
profile*.out.log

# Per-run output directories (controls, fidelity curves, figs)
robust_iswap_detuned_*/

# Knot-sweep output directories
knot_sweep_*/
